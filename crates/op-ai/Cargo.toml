[package]
name = "op-ai"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "OpenPencil AI chat layer — transport-free ChatProvider trait + model catalog + agent-settings state"

[lib]
name = "op_ai"
path = "src/lib.rs"

[dependencies]
# The only dependency: `chat_sse`'s payload parsing. Kept workspace-pinned
# and wasm32-clean; real transports stay host-side.
serde_json = { workspace = true }
