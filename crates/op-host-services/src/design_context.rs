//! Re-export shim: the canvas consistency brief moved to `op-chat-agent`
//! (pure code motion) so mobile design turns get the same screen-2+
//! product-consistency context; existing paths stay valid.

pub use op_chat_agent::design_context::*;
